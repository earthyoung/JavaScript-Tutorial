export const capitalizeString = str => str.toUpperCase();

export const capitalizeOneString = (string) => {
  return string.charAt(0).toUpperCase() + string.slice(1);
}

export const foo = "bar";
export const bar = "foo";
